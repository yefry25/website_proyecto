import { Routes } from '@angular/router';
import { VideoGameComponent } from './features/video-game/video-game.component';

export const routes: Routes = [
  { path: 'video-game', component: VideoGameComponent }
];
